spring:
  application:
    name: login-processing-service
  datasource:
    url: jdbc:postgresql://localhost:5432/loginprocessingdb?currentSchema=public
    username: postgres
    password: postgres
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
